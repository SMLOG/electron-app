export const i3 = {
  findMaxMin: function(t) {
    0 == t.length && (t = [0]);
    var e = t.slice().filter(function(t) {
      return void 0 != t && null != t && "" != t;
    });
    return (
      0 == e.length && (e = [0]),
      e.sort(function(t, e) {
        return t - e;
      }),
      { max: e[e.length - 1] / 1, min: e[0] / 1 }
    );
  },
};
